# Framespace

HyperFrames 工作台 Windows 桌面版。

## 运行

开发：

```bat
cd 1.0.1
npm install
npm start
```

打包便携版 exe：

```bat
cd 1.0.1
npm run dist
```

生成文件：`1.0.1/dist/Framespace-1.0.1.exe`

## 用法

双击 exe 打开。在项目库选工程后，可预览、抽帧、打开 Grok / ChatGPT / Codex CLI / Claude Code / Cursor。